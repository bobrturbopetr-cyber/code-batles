{
  "name": "codebattles-server",
  "version": "1.0.0",
  "description": "CodeBattles Global Backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "server": "json-server --watch db.json --port 3000 --middlewares server.js"
  },
  "dependencies": {
    "json-server": "^0.17.4",
    "cors": "^2.8.5"
  }
}
